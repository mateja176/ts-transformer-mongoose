{
  "name": "ts-transformer-mongoose",
  "version": "0.2.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "yarn build --watch",
    "deploy": "yarn version --minor && cp package.json build && yarn publish build"
  },
  "keywords": [],
  "author": "Mateja Petrovic <mateja176@gmail.com> (https://github.com/mateja176)",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.6.3"
  },
  "devDependencies": {
    "nodemon": "^1.19.3",
    "ts-node": "^8.4.1"
  },
  "peerDependencies": {
    "typescript": "^3.4.3"
  }
}
